export const selectorTodo = (state) => state.todos.todos;
export const selectorEditingTodo = (state) => state.todos.nowEditing;
export const selectorTimer = (state) => state.timer;
export const selectorModal = (state) => state.modal;
export const selectorAlarm = (state) => state.alarm;
export const selectorChart = (state) => state.chart;
